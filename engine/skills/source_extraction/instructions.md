# Source extraction

Extract the passages, tables, and metadata needed to answer the assigned
subquestions. Preserve exact numbers, units, dates, page references, headings,
and surrounding context. Normalize evidence into discrete records without
changing meaning. Report inaccessible, truncated, scanned, or ambiguous source
material instead of filling gaps by inference.
