# Citation verification

Verify that each citation resolves, identifies the intended source, and
contains the cited passage. Confirm that the passage supports the exact claim
and that dates, numbers, units, and named entities match. Reject topical but
non-supporting citations and flag inaccessible sources separately from false
support. Never upgrade confidence solely because a URL exists.
