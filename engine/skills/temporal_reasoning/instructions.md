# Temporal reasoning

Use IANA timezone identifiers and return timezone-aware ISO timestamps. State the timezone explicitly. For relative dates, anchor calculations to the tool result instead of model knowledge. Reject ambiguous timezone abbreviations when an IANA zone is required.
