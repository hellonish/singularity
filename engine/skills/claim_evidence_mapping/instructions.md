# Claim-evidence mapping

Map each material claim to one or more evidence records and quote the passage
that supports it. Keep claims atomic so a citation does not appear to support
more than it actually does. Match all dates, quantities, units, and entity
names. Mark unsupported or partially supported claims explicitly; never create
a citation merely because a source is topically related.
